# icare-lms-new

A React-based web application with an integrated medical image viewer (`viewer`).

## Development

To start the development server for both the application and the viewer, run:
```bash
npm run dev-all
```

To build the project:
```bash
npm run build-all
```
